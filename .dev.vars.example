#
# Copy to `.dev.vars` for local development:
#   cp .dev.vars.example .dev.vars
#
# Wrangler will load these when you run `wrangler dev`.
# NOTE: `.dev.vars` is ignored by git. Do NOT commit real secrets.
#

# Worker-side auth (required)
# WORKER_AUTH_KEY=your-worker-auth-key
# Optional: multiple client auth keys (comma-separated)
# WORKER_AUTH_KEYS=key1,key2,key3

# Config-driven routing (required)
# One-line JSON is easiest for .dev.vars; for production prefer the multi-line TOML string in `wrangler.toml.example`.
#
# Single-upstream example:
# RSP4COPILOT_CONFIG={"version":1,"providers":{"openai":{"apiMode":"openai-responses","baseURL":"https://your-relay.example/openai","apiKey":"REPLACE_ME","models":{"gpt-5.2":{"upstreamModel":"gpt-5.2"}}}}}
#
# Multi-upstream aggregation example (round-robin):
# RSP4COPILOT_CONFIG={"version":1,"providers":{"openai":{"apiMode":"openai-responses","routing":{"strategy":"round_robin"},"upstreams":[{"id":"relay-a","baseURL":"https://relay-a.example/openai","apiKey":"REPLACE_ME"},{"id":"relay-b","baseURL":"https://relay-b.example/openai","apiKey":"REPLACE_ME"}],"models":{"gpt-5.2":{"upstreamModel":"gpt-5.2"}}}}}
RSP4COPILOT_CONFIG={"version":1,"providers":{"openai":{"apiMode":"openai-responses","baseURL":"https://your-relay.example/openai","apiKey":"REPLACE_ME","models":{"gpt-5.2":{"upstreamModel":"gpt-5.2"}}}}}

# Debug logging
# RSP4COPILOT_DEBUG=true

# Chat history limits (optional; defaults shown; set 0 to disable)
# RSP4COPILOT_MAX_TURNS=40
# RSP4COPILOT_MAX_MESSAGES=200
# DEFAULT_RSP4COPILOT_MAX_INPUT_CHARS=300000
# RSP4COPILOT_MAX_INPUT_CHARS=300000
